# FR3 仿真控制系统 · 期末项目最终方案

**任务**:堆积木(垂直堆叠 3 块)
**课程**:机器人建模与控制
**团队**:6 人 · 4 周
**仿真平台**:MuJoCo + Python
**机械臂**:Franka Research 3(7-DOF)+ Franka Hand

---

## 一、整体思路与答辩叙事

### 项目定位

**从零搭建一个 FR3 仿真控制系统,以"堆积木"任务为载体,完整覆盖建模、控制、规划三大课程核心,自主推导与实现关键算法,并扩展到 learning-based 控制方法。**

### 答辩一句话

> "我们没有把 FR3 当黑箱去调用,而是从 DH 参数开始,自己推导运动学、自己写 IK 求解器、自己实现并对比了多种控制器,最终用堆积木任务串起所有模块,并用 learning 方法做了对比扩展。"

### 任务说明:堆积木

将 3 块积木垂直堆叠成塔,要求堆叠偏移误差 < 5mm,塔体保持 3 秒不倒。这个任务对项目模块有天然映射:

- 笛卡尔直线轨迹规划 → M3
- 多步任务协同 → M6
- 末端精确定位 → M2 + M4
- 放置稳定性 → M4 阻抗控制的优势体现

### 为什么选这个方案(现实考量)

**为什么纯仿真?** 真机环境(franka_ros2、libfranka)在 4 周内配通的风险过高。纯仿真把"工程踩坑"的时间换成"算法实现与对比",更贴课程主题。

**为什么不用 MoveIt 之类的现成框架?** MoveIt 把 IK、规划、控制都封装成黑箱。**自己写一遍才是这门课该做的事**。

**为什么选 MuJoCo?** 装起来简单(`pip install mujoco`)、`mujoco_menagerie` 提供高质量 FR3 模型、接触动力学精度高、Python 接口干净。

**为什么不进视觉模块?** 仿真里积木位置可以预设已知,视觉是"自找麻烦"。视觉的故事在真机上才有意义。

**为什么 learning 保留为扩展?** Learning 是当下机器人领域的主流方向之一,加一个"经典控制 vs 学习控制"的对比,既体现广度又有深度。

### 项目结构总览

```
M1 正运动学建模      DH + FK + 雅可比                     课程核心
M2 逆运动学求解      数值解多种方法对比                    课程核心 · 深度抓手
M3 轨迹规划          关节空间 + 笛卡尔空间 + 多种插值       课程核心
M4 控制器设计        PD + 计算力矩 + 阻抗,三种对比         课程核心 · 深度抓手
M5 仿真环境搭建      MuJoCo + FR3 + 积木场景               工程基础
M6 堆积木任务集成    完整任务 pipeline + 评测              综合演示
M7 学习方法扩展      Behavior Cloning vs 经典控制对比      扩展
```

M1-M6 是主线必做,M7 是扩展争取做。

### 任务参数约定(全员对齐)

为了模块间接口一致,以下参数所有人按统一规则使用:

| 参数 | 值 | 备注 |
|---|---|---|
| 积木尺寸 | 5cm × 5cm × 5cm | 立方体,统一规格 |
| 积木数量 | 3 块 | 标号 B1、B2、B3 |
| 初始摆放 | 桌面预设位置(可随机化) | 仿真中坐标已知,无需视觉 |
| 目标塔位置 | 桌面固定坐标(由 M5 定义) | 全员共享 |
| 堆叠形态 | 垂直堆叠 3 层 | 简单优先 |
| 精度要求 | 堆叠偏移 < 5mm | 评测指标 |
| 稳定性要求 | 堆叠完成后保持 3 秒不倒 | MuJoCo 物理仿真自然评测 |

---

## 二、各成员负责板块详细规划

每个板块下都包含:任务目标、技术栈、基本目标、加分项、个人时间表、报告产出。

---

### M1 · 正运动学建模 · wyb

#### 任务目标

推导 FR3 的 DH 参数,实现正运动学(FK)和雅可比矩阵(J),为后续 IK 和控制器提供基础工具。

#### 技术栈

- Python + NumPy
- SymPy(符号推导,可选,用于自动生成雅可比)
- MuJoCo(用于结果验证)

#### 基本目标(必达)

- [ ] FR3 改进 DH 参数表整理,与官方文档/URDF 交叉验证
- [ ] 正运动学函数:输入 7 个关节角 q ∈ ℝ⁷,输出末端位姿(齐次变换矩阵 T ∈ SE(3))
- [ ] 几何雅可比矩阵 J(q) ∈ ℝ^(6×7) 实现
- [ ] **验证**:在 100 个随机关节角下,自实现 FK 与 MuJoCo 内置 FK 的位置误差 < 1e-6 m,姿态误差 < 1e-6 rad
- [ ] **接口规范**:封装为 `kinematics.py` 模块,提供 `forward_kinematics(q)`、`jacobian(q)` 函数

#### 加分项

- [ ] 解析雅可比(对欧拉角导数,而非角速度)
- [ ] 雅可比的奇异值分解,可视化奇异位形
- [ ] 与 `pinocchio` 库的结果对比

#### 个人时间表

| 周次 | 任务 |
|---|---|
| **第 1 周** | DH 参数推导 + FK 实现 + 与 MuJoCo 对比验证 |
| **第 2 周** | 雅可比实现 + 验证 + 给 M2/M4 提供稳定接口 |
| **第 3 周** | 协助 M4 控制器实现(他们要用雅可比)+ 加分项 |
| **第 4 周** | 报告章节撰写 + 整理 DH 参数图表 |

#### 报告产出

- DH 参数表 + 坐标系示意图
- FK 推导过程
- 雅可比推导过程(几何法)
- 与 MuJoCo 对比的误差表

---

### M2 · 逆运动学数值求解对比 · xy

#### 任务目标

实现 FR3 的逆运动学求解,处理 7-DOF 冗余,对比多种数值方法。**这是项目的主要深度抓手之一**。

#### 技术栈

- Python + NumPy + SciPy
- 依赖 M1 的 FK 与雅可比

#### 基本目标(必达)

- [ ] **数值解 1**:Jacobian Pseudoinverse(伪逆迭代法)
- [ ] **数值解 2**:Damped Least Squares(DLS,处理奇异点)
- [ ] 冗余自由度的零空间投影 → 实现"在末端不变的前提下移动肘部"
- [ ] **对比实验**:针对堆积木任务的关键位姿(抓取、抬起、放置)各取 5 个目标位姿,统计两种方法的:
  - 迭代次数
  - 收敛精度(末端位姿误差)
  - 计算时间
  - 收敛失败率
- [ ] 奇异点附近的稳定性测试
- [ ] **接口规范**:`ik.py`,提供 `inverse_kinematics(target_pose, q_init, method='dls') → q` 函数

#### 加分项

- [ ] **半解析解**:基于 7-DOF 冗余分解(肘角参数化),实现一个混合 IK
- [ ] 在零空间里加优化目标(避关节极限、避奇异)
- [ ] 与开源库 `pinocchio` 或 `PyRoKi` 的 IK 对比

#### 个人时间表

| 周次 | 任务 |
|---|---|
| **第 1 周** | 算法原理学习 + Pseudoinverse 初版实现 |
| **第 2 周** | DLS 实现 + 冗余处理 + 对比实验数据收集 |
| **第 3 周** | 加分项(零空间优化)+ 协助 M3/M6 调用 IK 接口 |
| **第 4 周** | 报告章节撰写 + 实验图表整理 |

#### 报告产出

- 两种 IK 算法的数学推导
- 收敛性、精度、计算时间对比表
- 奇异位形处理的对比
- 冗余自由度的演示(同一末端位姿、不同关节构型)

---

### M3 · 轨迹规划 · jzc

#### 任务目标

实现关节空间和笛卡尔空间的轨迹规划,**重点支持堆积木任务的笛卡尔直线轨迹要求**。

#### 技术栈

- Python + NumPy + SciPy
- 依赖 M1 的 FK + M2 的 IK

#### 基本目标(必达)

- [ ] **关节空间**:五次多项式插值(起止位置/速度/加速度可控)
- [ ] **关节空间**:梯形速度规划(LSPB)
- [ ] **笛卡尔空间**:位置直线插值 + 姿态 SLERP(任务目标 2 的硬要求)
- [ ] 速度、加速度曲线可视化
- [ ] **针对堆积木任务**:支持 "approach 阶段(慢)→ transfer 阶段(快)→ place 阶段(慢)" 的三段速度策略
- [ ] **对比实验**:同一抓取-放置任务下,关节插值 vs 笛卡尔插值的末端轨迹偏差
- [ ] **接口规范**:`trajectory.py`,提供 `plan_joint_trajectory(q_start, q_end, T)`、`plan_cartesian_trajectory(pose_start, pose_end, T)` 函数

#### 加分项

- [ ] B 样条 / 多段轨迹拼接(支持中间路径点)
- [ ] 在线避障(简单情况,如避开已堆叠的积木)
- [ ] 加加速度(jerk)优化

#### 个人时间表

| 周次 | 任务 |
|---|---|
| **第 1 周** | 关节空间五次多项式 + 梯形速度规划实现 |
| **第 2 周** | 笛卡尔空间直线 + SLERP 实现 + 接口对接 M2 |
| **第 3 周** | 三段速度策略针对堆积木调优 + 加分项 |
| **第 4 周** | 报告章节撰写 + 速度曲线可视化整理 |

#### 报告产出

- 各类规划方法的数学描述
- 关节 vs 笛卡尔规划的末端轨迹偏差对比
- 三段速度策略的设计与效果
- 速度/加速度曲线图

---

### M4 · 控制器设计与对比 · dzn

#### 任务目标

实现三种控制器,在堆积木任务上对比性能。**这是项目的主要深度抓手之一**。

#### 技术栈

- Python + MuJoCo + NumPy
- 依赖 M1 的 FK + 雅可比

#### 基本目标(必达)

- [ ] **控制器 1**:关节空间 PD 控制 + 重力补偿
- [ ] **控制器 2**:计算力矩控制(Computed Torque,使用 MuJoCo 的 `mj_rne` 获取动力学)
- [ ] **控制器 3**:笛卡尔空间阻抗控制
- [ ] 推导每个控制器的控制律,在报告中给出
- [ ] **针对堆积木任务的对比实验**:
  - 同一笛卡尔直线轨迹下的跟踪误差(RMSE)
  - 放置阶段末端速度收敛性(关键!直接影响 5mm 精度)
  - 干扰响应:在 transfer 阶段给末端施加横向脉冲外力
  - 三种控制器最终堆叠精度对比
- [ ] **接口规范**:`controllers.py`,统一 `controller(q, dq, target, t) → tau` 形式

#### 加分项

- [ ] 自适应阻抗(放置阶段降低 stiffness 提高柔顺性)
- [ ] 不同积木质量下的鲁棒性测试
- [ ] 滑模控制或其他高级控制器作为额外对比

#### 个人时间表

| 周次 | 任务 |
|---|---|
| **第 1 周** | 阅读相关文献 + PD + 重力补偿实现 |
| **第 2 周** | 计算力矩控制实现 + 推导文档 |
| **第 3 周** | 阻抗控制实现 + 干扰实验 + 三控制器对比 |
| **第 4 周** | 报告章节撰写(三种控制律推导 + 实验图表) |

#### 报告产出

- 三种控制律的完整数学推导
- 跟踪误差曲线对比图
- 干扰响应曲线对比图
- 堆叠精度统计表(每种控制器跑 N 次实验,统计成功率与平均误差)

---

### M5 + M6 · 仿真环境与堆积木任务实现 · jsy

> M5 和 M6 由同一人负责,因为环境和任务集成强耦合,合并执行效率最高。

#### 任务目标

**M5**:搭建可复用的 FR3 + 积木 MuJoCo 仿真环境,提供数据记录、可视化等基础设施。
**M6**:基于其他模块的成果,完成堆积木任务的端到端 pipeline,并按任务要求(< 5mm,3 秒不倒)进行评测。

#### 技术栈

- MuJoCo + `mujoco_menagerie/franka_fr3`
- Python + NumPy + matplotlib
- MJCF 格式编辑(XML)
- 依赖所有其他模块的接口

#### 基本目标(必达)

**M5 仿真环境**

- [ ] MuJoCo 加载 FR3 + Franka Hand 的完整模型
- [ ] 在场景中加入桌面 + 3 块积木(MJCF 编写)
- [ ] 统一控制接口:支持扭矩控制、位置控制、夹爪开合
- [ ] 数据记录工具:关节状态、末端位姿、控制量、积木位姿、时间戳(写入 CSV 或 npz)
- [ ] 可视化工具:matplotlib 画轨迹/误差曲线;MuJoCo viewer 录制 demo 视频
- [ ] 场景随机化接口:积木初始位置可随机但坐标已知

**M6 任务集成**

- [ ] **端到端 pipeline**:循环执行 3 次"抓取 → 抬起 → 移动 → 放置 → 松开 → 退回",完成 3 块积木堆叠
- [ ] 每个阶段调用对应模块:M2(IK)+ M3(轨迹)+ M4(控制)
- [ ] **评测脚本**:
  - 检查最终塔的偏移误差(读积木位姿计算)
  - 检查 3 秒不倒(物理仿真到任务结束后再跑 3 秒看是否倒塌)
  - 自动统计成功率(N 次实验)
- [ ] **三种控制器分别跑 N=20 次**,生成对比表
- [ ] 演示视频录制

#### 加分项

- [ ] 积木初始位置随机化下的鲁棒性测试
- [ ] 不同堆叠形态(金字塔)的扩展
- [ ] 失败案例分析(哪些情况会倒、为什么)
- [ ] 用 MuJoCo 的渲染功能生成简单的多视角 demo

#### 个人时间表

| 周次 | 任务 |
|---|---|
| **第 1 周** | M5 主体:MuJoCo + FR3 加载 + 积木场景 MJCF + 数据记录工具 |
| **第 2 周** | M5 完成:控制接口统一 + 可视化工具 + 提供给其他人测试用 |
| **第 3 周** | M6:端到端 pipeline 搭建 + 三控制器跑通 + 评测脚本 |
| **第 4 周** | M6 完成:批量实验 + demo 视频 + 报告章节 |

#### 报告产出

- 仿真场景说明与 MJCF 关键设计
- 端到端 pipeline 的状态机图
- 评测方法说明
- 堆叠精度、成功率统计表
- demo 视频

#### 集成责任(关键!)

jsy 作为 M5+M6 负责人,自动承担**集成协调**职责:

- **第 1 周末**就要给出统一接口规范(和 wyb、xy、jzc、dzn 商定)
- 每周收集其他模块的最新代码,做集成冒烟测试
- 发现接口不一致或 bug 时第一时间反馈

---

### M7 · 学习方法扩展(协作) · Leslie + xwy

#### 任务目标

用 Behavior Cloning 学一个堆积木策略,与经典控制器对比。**这是扩展模块,主线模块完成后再投入精力**。

#### 技术栈

- Python + PyTorch + MuJoCo
- 依赖 M5 的仿真环境 + M2-M4 的 scripted expert

#### 基本目标(必达)

- [ ] 用 M2-M4 的经典控制器作为 scripted expert,在堆积木任务上生成 100+ 条成功轨迹
- [ ] 训练 BC 网络(MLP):输入(关节状态 + 积木位姿)→ 输出动作(关节扭矩或末端位姿增量)
- [ ] 在仿真中评估 BC 策略,与经典控制器对比堆叠成功率
- [ ] 拟合精度分析、失败模式分析

#### 降级方案(若 BC 训不出可用策略)

报告改写成"BC 拟合精度分析 + 失败模式讨论",仍是合格的学术内容。**不要硬撑**。

#### 加分项

- [ ] 用 Diffusion Policy 简化版替代 MLP
- [ ] 加入 domain randomization(积木初始位置)看泛化性
- [ ] 用更小的数据量(50 条)看数据效率

#### 协作时间表

| 周次 | Leslie | xwy |
|---|---|---|
| **第 1 周** | 学习 BC 基础 + PyTorch 环境 | 启动开题报告 + 报告骨架 |
| **第 2 周** | 数据 pipeline 搭建(对接 M5 数据格式)| 写 M1、M2 章节初稿(基于其他人产出)|
| **第 3 周** | scripted expert 数据收集 + BC 网络搭建 | 写 M3、M4 章节初稿 |
| **第 4 周** | BC 训练 + 评估 + 章节撰写 | 整合所有章节 + PPT + 视频 |

> **Leslie 和 xwy 协作要点**:Leslie 偏算法实现和数据,xwy 偏文档和评估。第 4 周 Leslie 完成 BC 实验后把结果交给 xwy 整合到报告。

#### 报告产出

- BC 网络结构与训练设置
- 数据收集说明
- BC 策略的成功率与失败模式
- 与经典控制器的对比表

---

### 文档与项目管理 · xwy

#### 任务目标

负责所有文字材料的整合输出:开题报告、最终报告、PPT、演示视频。同时承担项目协调与机动救火。

#### 技术栈

- Markdown / LaTeX
- PowerPoint 或 Keynote
- 视频剪辑(剪映 / Premiere / DaVinci)
- 基础 Python(辅助跑实验出图)

#### 基本目标(必达)

**开题报告**(第 1 周末提交)

- [ ] 项目背景、目标、技术路线
- [ ] 模块划分与分工
- [ ] 时间安排
- [ ] 预期成果

**最终报告**

- [ ] 整合所有模块的章节(各模块负责人提供初稿,xwy 统稿)
- [ ] 实验数据章节(汇总各模块数据)
- [ ] 总结与展望
- [ ] 排版、参考文献、附录

**演示 PPT**

- [ ] 项目概览
- [ ] 各模块亮点(每模块 1-2 页)
- [ ] 实验结果(图表为主)
- [ ] 总结

**演示视频**

- [ ] 5 分钟左右,展示堆积木任务完整流程
- [ ] 三种控制器对比片段
- [ ] BC 策略对比片段(如果完成)

#### 协调职责

- 维护周报机制(每周一收集各成员进展)
- 跟进 jsy 的接口规范确认(第 1 周末)
- 第 3 周末协助 jsy 做 go/no-go 决策(数据支持)
- 哪个模块卡住了去补位救火

#### 个人时间表

| 周次 | 任务 |
|---|---|
| **第 1 周** | 开题报告主笔 + 报告骨架搭建 + 周报机制启动 |
| **第 2 周** | M1、M2 章节初稿(基于 wyb、xy 的产出)+ 协助 Leslie 做 M7 |
| **第 3 周** | M3、M4 章节初稿(基于 jzc、dzn 的产出)+ 视频素材开始拍 |
| **第 4 周** | 整合所有章节 + PPT 制作 + 视频剪辑 + 答辩排练组织 |

#### 报告分章节责任

| 章节 | 主笔 | xwy 责任 |
|---|---|---|
| 摘要 / 项目概览 | xwy | 主笔 |
| 系统架构与仿真环境 | jsy | 整理 |
| 运动学建模(M1) | wyb | 整理 |
| 逆运动学(M2) | xy | 整理 |
| 轨迹规划(M3) | jzc | 整理 |
| 控制器设计(M4) | dzn | 整理 |
| 任务集成与实验(M6) | jsy | 整理 |
| 学习方法扩展(M7) | Leslie | 整理 |
| 结论与展望 | xwy | 主笔 |

---

## 三、需要队内敲定的讨论点

会议时按这个顺序过一遍,**最好第一次会议(本周内)就 settle**:

1. **MuJoCo Python binding 用官方 `mujoco` 还是 `dm_control`?**
   推荐官方 `mujoco`,资料新、依赖少。

2. **DH 参数用标准 DH 还是改进 DH(Modified DH)?**
   推荐改进 DH——FR3 官方文档用的是改进 DH,可以直接对照验证。

3. **统一接口规范由 jsy 拍板,**第 1 周末锁定**,后续不许改。
   关键接口:`forward_kinematics(q)`、`jacobian(q)`、`inverse_kinematics(...)`、`plan_*_trajectory(...)`、`controller(q, dq, target, t)`。

4. **第 3 周末 go/no-go 决策由谁拍板?**
   建议 jsy(集成责任人)拍板,xwy 提供进度数据支持。

5. **报告交付物格式与字数要求?**
   待 xwy 跟老师确认,**第 1 周内必须搞清楚**。

6. **如果时间不够,M7 砍还是 M4 阻抗砍?**
   M4 阻抗优先保(课程内容),M7 优先砍(扩展)。

7. **是否给最后真机预留缓冲?**
   原计划"仿真效果好就上真机"——4 周内不太现实。建议**作为答辩后/未来工作来讲**,不进入 4 周计划。如果第 4 周真的有余量,6 个人一起冲一次。

---

## 四、风险清单与对策

| 风险 | 概率 | 对策 |
|---|---|---|
| 第 1 周 MuJoCo 模型加载有问题 | 低 | jsy 第 1 周专注此事,1 天搞不定就求助 |
| IK 数值解收敛慢/不稳 | 中 | xy 备好两种方法(伪逆 + DLS),交叉验证 |
| 计算力矩控制需要的动力学参数从哪来? | 中 | 直接读 MuJoCo 的 `mj_rne` 或 `mj_fullM`,不自己推 |
| 模块之间接口不统一 | 中 | jsy 第 1 周末锁定接口规范 |
| BC 训不出可用策略 | 高 | 已预设降级方案 |
| 报告写晚 | 高 | xwy 第 2 周开始写,不可推迟 |
| 堆积木 5mm 精度达不到 | 中 | 优先用阻抗控制器,放置阶段降速;若仍不行,放宽到 1cm 也是可接受的实验结果 |
| 物理稳定性失败(倒塌) | 中 | 调整 MuJoCo 的接触摩擦参数,这本身就是一个有趣的报告内容 |

---

## 五、执行纪律

1. M1-M6 是主线,M7 是扩展,任何时候主线优先
2. 接口规范第 1 周末由 jsy 锁定,后续不许改
3. 报告第 2 周开始写
4. 第 3 周末 go/no-go 由 jsy 拍板
5. 第 4 周周三后不再做新实验,只整理
6. 加分项不做不丢分,基本目标必须达成
7. 任何环节延期超过 1 天必须在群里报警
8. 真机复现作为 stretch goal,只在所有主线完成后才考虑
